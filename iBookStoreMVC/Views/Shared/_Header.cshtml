@model IEnumerable<iBookStoreMVC.ViewModels.Header>

<div class="page-header">
    @foreach (var header in @Model) {
        <a class="page-header-back" asp-area="" asp-controller="@header.Controller" asp-action="Index">@header.Text</a>
    }
</div>